# API base URL used by the built static site (no trailing slash).
# Leave empty to use same origin as the web UI (recommended behind one reverse proxy).
VITE_API_BASE_URL=

# Dev/preview proxy target when VITE_API_BASE_URL is empty.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
